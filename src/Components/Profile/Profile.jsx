import React from "react";
import styles from "./styles/Profile.module.scss";

const Profile = (props) => {
  console.log(styles.container);
  return <div className={styles.container}>dsads</div>;
};

export default Profile;
